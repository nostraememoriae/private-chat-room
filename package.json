{
  "name": "private-chat-room",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "reset:local": "rm -rf .wrangler"
  },
  "dependencies": {
    "hono": "^4.11.9"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.25.1",
    "@sveltejs/vite-plugin-svelte": "^6.2.4",
    "svelte": "^5.51.3",
    "svelte-check": "^4.4.0",
    "vite": "^7.3.1",
    "wrangler": "^4.66.0"
  }
}